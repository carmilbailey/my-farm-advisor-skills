- 2026-04-28: `scripts/validate.sh` is bootstrap-safe: absent expected skill directories, root provenance scaffolding, and geoadmin metadata report as WARN until imports land; legacy copied-skill directories and tracked generated paths/assets remain hard FAIL checks.
- 2026-04-28: For `my-farm-qtl-analysis`, the safest import pattern was local-first structure + remote-only backfill. The local grouped taxonomy stayed intact, remote README/CLI completeness was merged in, and all `.npy`/`.npz`/`.raw` candidates turned out to live under generated `output/` trees that should be pruned instead of LFS-tracked.
- 2026-04-28: `my-farm-advisor/` imports cleanly from upstream `skills/my-farm-advisor/` with `rsync -a --delete` when `r2-seed-pipeline/src/shared/geoadmin/l0_countries/countries.geojson` and `r2-seed-pipeline/src/shared/geoadmin/l1_states/states_usa.geojson` are explicitly excluded; keep the three geoadmin `metadata.json` files and the pipeline entrypoints/downloader script in QA evidence.
- 2026-04-28: `scripts/validate.sh` hard-fails any already-present expected skill directory that lacks a root `README.md`; `my-farm-breeding-trial-management/` needed a bootstrap README before the repo could validate cleanly.
- 2026-04-28: The upstream `my-farm-advisor` umbrella tree also carries generated maturity report/table artifacts under `r2-seed-pipeline/src/shared/*/{reports,tables}/`; exclude those alongside the large geoadmin payloads to avoid committing runtime outputs.
- 2026-04-28: For local-skill imports with better grouped taxonomy than the remote baseline, copy the local worktree verbatim first, then backfill only remote-only completeness items (README/CLI/examples) and document the remote SHA separately so grouped paths and adjusted references stay intact.
